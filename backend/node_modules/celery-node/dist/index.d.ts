import Client from "celery-node/dist/app/client";
import Worker from "celery-node/dist/app/worker";
/**
 * @description Basic function for creating celery client
 *
 * @function
 * @returns {Client}
 */
export declare function createClient(broker?: string, backend?: string): Client;
/**
 * @description Basic function for creating celery worker
 *
 * @function
 * @returns {Worker}
 */
export declare function createWorker(broker?: string, backend?: string): Worker;
